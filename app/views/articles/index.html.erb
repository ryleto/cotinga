<% provide(:title, 'News Archive') %>
<h1>Articles</h1>

<%= will_paginate %>

<ul class="users">
  
  <% @articles.each do |article| %>
    <li><%= link_to article.title, article %> <small><%= article.pubdate %></small></li>
  <% end %>
</ul>

<%= will_paginate %>